{% extends 'common/wrapper' %}

{% set underHeader = true %}

{% block main %}
  {% for pageBlock in entry.pageBlocks.all() %}
    {{ include('blocks/' ~ pageBlock.type) }}
  {% endfor %}
{% endblock %}
